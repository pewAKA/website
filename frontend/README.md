# website frontend

个人网站前端项目，基于 React、TypeScript、Vite、Sass、React Router、Axios、Ant Design、Lodash 和 React Three Fiber 初始化。

## 开发命令

```bash
pnpm install
pnpm dev
pnpm build
pnpm format:check
```

## 环境变量

复制 `.env.example` 为本地 `.env` 后按需调整：

```bash
VITE_API_BASE_URL=/api
```
